# phlsphy blog powered by Nikola, using a staticsite template.

This is such a pain.

Create shortcodes(?) for links, projects...
`nikola build -a` to force build everything.
`nikola auto -b` to enable automatic rebuilds.